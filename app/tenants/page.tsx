export default function TenantsPage() {
  return (
    <main className="page">
      <section className="page__header">
        <div>
          <h1 className="page__title">Tenants</h1>
          <p className="page__subtitle">Tenant CRUD will be wired once the backend owner/tenant routes are added.</p>
        </div>
      </section>
      <div className="empty">This page is a shell for the next implementation phase.</div>
    </main>
  );
}

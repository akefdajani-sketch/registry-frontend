export default function OwnersPage() {
  return (
    <main className="page">
      <section className="page__header">
        <div>
          <h1 className="page__title">Owners</h1>
          <p className="page__subtitle">Owner CRUD is the next backend module to wire after properties.</p>
        </div>
      </section>
      <div className="empty">This page is intentionally held as a shell until owner routes are added to the registry backend.</div>
    </main>
  );
}
